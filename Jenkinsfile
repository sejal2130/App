pipeline {
    agent any

    stages {
        stage 'stage one') {
            steps {
                git branch: 'master',
                  url: 'https://github.com/sejal2130/App.git'
            }
        }
    }
}
